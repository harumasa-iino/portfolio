<div class="container">
  <h1 class="mt-4 mb-4">アップロードした画像</h1>
  <div class="row">
    <% if @room.image.present? %>
      <div class="image-container" style="position: relative;">
        <%= image_tag @room.image.url, class: "img-fluid mb-4" %>
        <div id="poster-frame" style="position: absolute; top: <%= @room.y_coordinate %>%; left: <%= @room.x_coordinate %>%;"></div>
        </div>
      <div class="d-grid gap-2 d-md-block mb-4">
        <%= form_with(model: @room, local: true, html: { class: "d-inline" }) do |form| %>
          <%= form.hidden_field :x_coordinate, id: 'poster_x' %>
          <%= form.hidden_field :y_coordinate, id: 'poster_y' %>
          <%= form.submit "ポスターの位置を保存", class: "btn btn-success btn-sm" %>
        <% end %>
        <p class="card-text stylish-text">ポスターの配置位置を確認したら好みのポスターのマッチング方法を選んでください</p>
      </div>
  </div>
  <h2>ポスターマッチングオプション</h2>
  <div class="row justify-content-between">
    <div class="col mb-5">
      <div class="card stylish-card">
        <%= link_to question_path(1), class: "card-body text-center" do %>
          <i class="fas fa-thin fa-filter filter-icon" style="font-size: 4em;"></i>
          <h5 class="card-title stylish-title">フィルターモード</h5>
          <p class="card-text stylish-text">好みのデザインタイプを選択してマッチング</p>
        <% end %>
      </div>
    </div>
    <div class="col mb-5">
      <div class="card stylish-card">
        <%= link_to composite_images_path, class: "card-body text-center" do %>
          <i class="fas fa-robot recommend-icon" style="font-size: 4em;"></i>
          <h5 class="card-title stylish-title">レコメンドモード</h5>
          <p class="card-text stylish-text">インテリアの色彩から自動マッチング</p>
        <% end %>
      </div>
    </div>
  </div>
  <h2>AIジェネレーターオプション</h2>
  <div class="row justify-content-between">
    <div class="col md-5">
      <div class="card stylish-card">
        <%= link_to "#", class: "card-body text-center" do %>
          <i class="fas fa-magic ai-icon" style="font-size: 4em; color: #6f42c1;"></i>
          <h5 class="card-title stylish-title">AIVision</h5>
          <p class="card-text stylish-text">AIがランダムでポスターを提案します</p>
        <% end %>
      </div>
    </div>
  </div>
  <div class="d-grid gap-2 d-md-block mb-4">
    <%= link_to "別の写真をアップロードする", new_room_path, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-danger btn-sm btn-margin" %>
  </div>
  <% else %>
    <p>画像がアップロードされていません。再試行してください</p>
  <% end %>
</div>